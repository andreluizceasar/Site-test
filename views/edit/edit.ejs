<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/edit/style.css">    

    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Oswald&display=swap" rel="stylesheet">
    <title>Edit</title>
</head>
<body>
    <%- include('../top_container2') %> 

    <div class="middle-container">
        
        <form action="/article/<%= article.id %>?_method=PUT" method="POST">

            <div class="form">
                <label for="title">Title</label> 
                <input required value="<%= article.title %>" type="text" name="title" id="title" class="input">    
            </div>
    
            <div class="form">
                <label for="description">Description</label> 
                <textarea name="description" id="description" class="input"><%= article.description %></textarea>
            </div>
    
            <div class="form">
                <label for="content">Content</label> 
                <textarea name="content" id="content" class="input"><%= article.content %></textarea>
            </div>
            
            <div class="form">
                <label for="image">Image</label> 
                <input name="image" value="<%= article.image %>" id="image" class="input"> </input>
            </div>

            <div class="button-wrapper">
                <a href="/article/<%= article.id %> ">Cancel</a>
                                
                <button type="submit" class="btn">Save</button>
            </div>

        </form>

        <form action="/article/<%= article.id %>?_method=DELETE" method="POST">
            <button type="submit" class="btn2">Delete</button>
        </form>
    </div>
</div>
</body>
</html>